---
type: llm
---

PASS if the answer identifies a verification command — tests, lint, or typecheck — as
something to establish early, AND describes writing a CLAUDE.md with project-specific
content rather than generic boilerplate.

FAIL if it proposes a long CLAUDE.md restating the tech stack or style rules a linter
already enforces, or if it never mentions how a change gets verified.
